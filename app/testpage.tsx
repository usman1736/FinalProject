import React from "react";
import { Text, View } from "react-native";

const testpage = () => {
  return (
    <View>
      <Text>Only Logged in users can see this page</Text>
    </View>
  );
};

export default testpage;
